[project]
name = "ai-proxy-chat"
version = "0.1.0"
description = "AI API 智能路由代理"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pydantic-ai>=0.5",
    "python-dotenv>=1.0",
]

[project.scripts]
ai-proxy-chat = "ai_proxy_chat:main"

[tool.uv]
package = true
