Metadata-Version: 2.4
Name: ai-proxy-chat
Version: 0.1.0
Summary: AI API 智能路由代理
Requires-Python: >=3.12
Description-Content-Type: text/markdown
Requires-Dist: httpx>=0.28.1
Requires-Dist: pydantic-ai>=0.5
Requires-Dist: python-dotenv>=1.0

# AI API 智能路由代理

根据请求是否包含 `tools` 字段，自动在**官方 API** 和**逆向 API** 之间智能路由，在保证功能完整性的同时优化 API 调用成本。

## 痛点

使用 AI API 时，官方 API 支持 tool calling（函数调用）但价格较高，逆向 API 价格低廉但不支持 tool calling。日常对话中有大量请求不需要 tool calling，却仍然支付了全价。

这个代理自动区分这两种场景：需要 tool calling 的请求走官方 API，纯对话走逆向 API，对客户端完全透明。

## 工作原理

```
客户端请求 → localhost:8000
         ↓
    检测 tools 字段？
         ↓
    ├── 有 tools → 官方 API（支持 tool calling，功能完整）
    └── 无 tools → 逆向 API（纯对话，节省成本）
         ↓
    响应原样返回客户端
```

每次请求独立判断，一个会话中既有工具调用又有纯对话时自动切换。

## 功能

- **智能路由** — 根据请求中的 `tools` 字段自动选择目标 API
- **流式支持** — 完整转发 Server-Sent Events (SSE) 流式响应
- **透明代理** — 响应格式与直接调用 API 完全一致，客户端无需修改
- **多格式兼容** — 同时支持 Anthropic 和 OpenAI API 格式
- **调试模式** — 详细日志输出，便于排查问题
- **请求追踪** — 每个请求分配唯一 ID，方便链路追踪
- **统计监控** — 内置请求统计和健康检查接口

## 快速开始

### 1. 安装

```bash
pip install -r requirements.txt
```

### 2. 配置

```bash
cp .env.example .env
```

编辑 `.env` 文件：

```env
# 官方 API（支持 tools，如 Anthropic API）
OFFICIAL_API_URL=https://api.anthropic.com/v1/messages
OFFICIAL_API_KEY=sk-ant-xxxxx

# 逆向 API（不支持 tools，成本更低）
REVERSE_API_URL=https://your-reverse-api.com/v1/chat/completions
REVERSE_API_KEY=your_key_here
REVERSE_API_MODEL=gpt-5.2

# 可选：开启调试模式
DEBUG_MODE=false
```

### 3. 启动

```bash
python api_proxy.py
```

服务将在 `http://localhost:8000` 启动。

## 配置项

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `OFFICIAL_API_URL` | 官方 API 地址（支持 tools） | `https://api.anthropic.com/v1/messages` |
| `OFFICIAL_API_KEY` | 官方 API 密钥 | - |
| `REVERSE_API_URL` | 逆向 API 地址（不支持 tools） | - |
| `REVERSE_API_KEY` | 逆向 API 密钥 | - |
| `REVERSE_API_MODEL` | 逆向 API 使用的模型名称 | `gpt-5.2` |
| `DEBUG_MODE` | 调试模式（true/false） | `false` |

## API 接口

### 代理接口

| 路由 | 说明 |
|------|------|
| `POST /v1/messages` | Anthropic 格式 |
| `POST /v1/chat/completions` | OpenAI 格式 |
| `POST /chat` | 通用入口（推荐） |

### 管理接口

| 路由 | 说明 |
|------|------|
| `GET /health` | 健康检查 |
| `GET /stats` | 请求统计 |
| `GET /debug/config` | 配置信息（隐藏密钥） |

## 与 Claude Code 集成

在 Claude Code 中指定 API endpoint 指向本代理：

```bash
# 方式一：启动时指定
claude --api-url http://localhost:8000

# 方式二：环境变量
export ANTHROPIC_API_URL=http://localhost:8000
```

## 示例

### 纯对话（路由到逆向 API）

```bash
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-3-5-sonnet-20241022",
    "messages": [{"role": "user", "content": "什么是快速排序？"}],
    "max_tokens": 500
  }'
```

### 工具调用（路由到官方 API）

```bash
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-3-5-sonnet-20241022",
    "messages": [{"role": "user", "content": "读取当前目录下的 config.json 文件"}],
    "max_tokens": 500,
    "tools": [
      {
        "name": "read_file",
        "description": "读取文件内容",
        "input_schema": {
          "type": "object",
          "properties": {
            "path": {"type": "string"}
          }
        }
      }
    ]
  }'
```

### 流式请求

```bash
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-3-5-sonnet-20241022",
    "messages": [{"role": "user", "content": "从 1 数到 5"}],
    "max_tokens": 100,
    "stream": true
  }' --no-buffer
```

## 测试

```bash
# 运行完整测试套件（确保服务已启动）
python test_proxy.py
```

测试内容：
1. 健康检查
2. 配置检查
3. 无 tools 请求（预期路由到逆向 API）
4. 有 tools 请求（预期路由到官方 API）
5. 流式请求
6. 统计信息

## 调试

开启调试模式获取详细的请求/响应日志：

```bash
DEBUG_MODE=true python api_proxy.py
```

查看调试信息：

```bash
# 健康检查
curl http://localhost:8000/health

# 查看配置（密钥仅显示是否已设置）
curl http://localhost:8000/debug/config

# 查看请求统计
curl http://localhost:8000/stats
```

详细调试指南见 [DEBUG_GUIDE.md](./DEBUG_GUIDE.md)。

## 项目结构

```
├── api_proxy.py        # 主程序（FastAPI 应用）
├── test_proxy.py       # 测试套件
├── PRD.md              # 产品需求文档
├── DEBUG_GUIDE.md      # 调试指南
├── requirements.txt    # Python 依赖
├── .env.example        # 环境变量模板
└── .env                # 本地配置（不提交到 Git）
```

**注意**：`.env` 文件包含 API 密钥，不应提交到版本控制。请确保已将其加入 `.gitignore`。

## 注意事项

1. **API 格式兼容** — 确保逆向 API 兼容 OpenAI 或 Anthropic 的请求/响应格式
2. **模型替换** — 无 tools 请求转发到逆向 API 时，模型名称会被替换为 `REVERSE_API_MODEL` 配置的值
3. **超时设置** — 默认 300 秒，可在 `forward_request` 函数中调整
4. **成本估算** — 以 70% 请求走逆向 API 计算，预计节省 30-50% 费用

## 常见问题

**服务无法启动（端口占用）**
```bash
netstat -ano | findstr :8000
taskkill /PID <进程ID> /F
```

**Claude Code 无法连接**
- 确认代理运行中：`curl http://localhost:8000/health`
- 检查防火墙是否放行 8000 端口

**请求总是路由到同一个 API**
- 检查请求体是否确实包含/不包含 `tools` 字段
- 开启 `DEBUG_MODE=true` 查看路由日志
- 检查 `.env` 中的 API key 是否已正确配置

**逆向 API 返回 401/403**
- 在 `/debug/config` 确认 API key 已设置
- 直接调用逆向 API 验证密钥有效性
- 某些逆向 API 可能需要额外的请求头

## License

MIT
