# PRD - AI API 智能路由代理

**文档版本：** v1.0  
**创建日期：** 2026-05-11  
**产品负责人：** [待填写]  
**状态：** 草稿

---

## 1. 产品概述

### 1.1 产品定义

AI API 智能路由代理是一个中间层服务，用于在多个大模型 API 之间智能路由请求。系统根据请求是否需要 tool calling 功能，自动选择最合适的 API endpoint，从而在保证功能完整性的同时优化成本。

### 1.2 产品价值

- **成本优化**：将不需要 tools 的简单对话路由到成本更低的逆向 API
- **功能保障**：需要 tool calling 的复杂任务自动路由到官方 API
- **透明切换**：对客户端完全透明，无需修改现有代码
- **统一接口**：支持 OpenAI 和 Anthropic 两种 API 格式

### 1.3 目标用户

- **个人开发者**：需要在成本和功能之间平衡的独立开发者
- **小型团队**：预算有限但需要完整 AI 功能的创业团队
- **AI 应用开发者**：使用 Claude Code/Codex 等工具的开发者
- **研究人员**：需要大量 API 调用但预算受限的研究人员

---

## 2. 业务目标

### 2.1 核心目标

1. **降低 API 使用成本 30-50%**
   - 将 70% 的简单对话请求路由到低成本 API
   - 仅在必要时使用官方 API

2. **保持 100% 功能完整性**
   - 所有需要 tool calling 的请求正确路由
   - 零功能降级

3. **实现透明代理**
   - 客户端无需修改代码
   - 响应格式完全兼容

### 2.2 成功指标

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 路由准确率 | ≥99.9% | 统计接口数据 |
| 响应延迟增加 | ≤50ms | 性能监控 |
| 成本节省 | ≥30% | 月度账单对比 |
| 服务可用性 | ≥99.5% | 健康检查日志 |
| 错误率 | ≤0.1% | 错误日志统计 |

---

## 3. 用户场景

### 3.1 场景一：日常代码咨询

**角色：** 开发者  
**场景：** 使用 Claude Code 进行代码咨询和解释

**流程：**
1. 开发者在 Claude Code 中询问："这段代码是什么意思？"
2. 请求发送到代理服务（无 tools 字段）
3. 代理路由到逆向 API
4. 返回解释结果
5. **成本节省：** 使用低成本 API

### 3.2 场景二：文件操作任务

**角色：** 开发者  
**场景：** 需要 Claude Code 读取或修改文件

**流程：**
1. 开发者请求："读取 config.json 文件"
2. Claude Code 发送请求（包含 tools 字段）
3. 代理检测到 tools 字段
4. 路由到官方 API
5. 执行 tool calling，完成文件操作
6. **功能保障：** 使用官方 API 确保功能完整

### 3.3 场景三：混合工作流

**角色：** 开发者  
**场景：** 一个会话中既有简单对话又有工具调用

**流程：**
1. 第一个请求："解释一下 React Hooks"（无 tools）→ 逆向 API
2. 第二个请求："创建一个新的 React 组件文件"（有 tools）→ 官方 API
3. 第三个请求："这个组件的作用是什么？"（无 tools）→ 逆向 API
4. **智能切换：** 每个请求独立判断，自动选择最优路由

---

## 4. 功能需求

### 4.1 核心功能

#### 4.1.1 智能路由（P0）

**功能描述：**
根据请求体中是否包含 `tools` 字段，自动选择目标 API。

**判断逻辑：**
```
IF request.tools 存在且非空:
    路由到官方 API
ELSE:
    路由到逆向 API
```

**验收标准：**
- ✅ 正确识别 tools 字段
- ✅ 路由决策准确率 ≥99.9%
- ✅ 决策延迟 ≤5ms

#### 4.1.2 请求转发（P0）

**功能描述：**
将客户端请求完整转发到目标 API，包括请求头、请求体和认证信息。

**技术要求：**
- 支持 POST 请求
- 保持请求体格式不变
- 正确设置认证头（x-api-key 或 Authorization）
- 支持超时配置（默认 300 秒）

**验收标准：**
- ✅ 请求体完整转发
- ✅ 认证信息正确设置
- ✅ 支持大请求体（≥1MB）

#### 4.1.3 响应返回（P0）

**功能描述：**
将上游 API 的响应原样返回给客户端。

**技术要求：**
- 保持响应状态码
- 保持响应头
- 保持响应体格式
- 支持流式响应

**验收标准：**
- ✅ 响应格式与直接调用 API 一致
- ✅ 流式响应正常工作
- ✅ 错误响应正确传递

#### 4.1.4 流式响应支持（P0）

**功能描述：**
支持 Server-Sent Events (SSE) 流式响应。

**技术要求：**
- 检测 `stream: true` 参数
- 使用流式转发
- 保持数据块完整性

**验收标准：**
- ✅ 流式数据实时传输
- ✅ 无数据丢失
- ✅ 正确处理流结束

### 4.2 监控功能

#### 4.2.1 健康检查（P0）

**接口：** `GET /health`

**响应示例：**
```json
{
  "status": "ok",
  "message": "API Proxy is running"
}
```

**验收标准：**
- ✅ 服务运行时返回 200
- ✅ 响应时间 ≤10ms

#### 4.2.2 统计信息（P1）

**接口：** `GET /stats`

**响应示例：**
```json
{
  "stats": {
    "total_requests": 1000,
    "official_api_requests": 300,
    "reverse_api_requests": 700,
    "errors": 5
  },
  "config": {
    "official_api": "https://api.anthropic.com/v1/messages",
    "reverse_api": "https://reverse-api.com/v1/chat/completions",
    "debug_mode": false
  }
}
```

**验收标准：**
- ✅ 实时统计准确
- ✅ 包含路由分布信息

#### 4.2.3 配置检查（P1）

**接口：** `GET /debug/config`

**功能描述：**
显示当前配置，隐藏敏感信息。

**验收标准：**
- ✅ 显示 API URLs
- ✅ 仅显示 API key 是否已设置
- ✅ 不泄露完整 API key

### 4.3 调试功能

#### 4.3.1 调试模式（P1）

**功能描述：**
通过环境变量 `DEBUG_MODE=true` 开启详细日志。

**日志内容：**
- 完整请求体
- 请求头（API key 截断）
- 响应预览
- 路由决策过程

**验收标准：**
- ✅ 日志信息完整
- ✅ 不泄露敏感信息
- ✅ 性能影响 ≤10%

#### 4.3.2 请求追踪（P1）

**功能描述：**
为每个请求生成唯一 ID，便于日志追踪。

**格式：** `req_{序号}_{时间戳}`

**验收标准：**
- ✅ 每个请求有唯一 ID
- ✅ 日志中包含请求 ID
- ✅ 便于问题排查

### 4.4 多路由支持（P0）

**支持的路由：**
- `POST /v1/messages` - Anthropic 格式
- `POST /v1/chat/completions` - OpenAI 格式
- `POST /chat` - 通用入口

**验收标准：**
- ✅ 所有路由正常工作
- ✅ 路由逻辑一致

---

## 5. 非功能需求

### 5.1 性能要求

| 指标 | 要求 | 说明 |
|------|------|------|
| 响应延迟 | ≤50ms | 代理层增加的延迟 |
| 吞吐量 | ≥100 req/s | 单实例处理能力 |
| 并发连接 | ≥1000 | 同时处理的连接数 |
| 内存占用 | ≤512MB | 正常运行时内存 |

### 5.2 可靠性要求

- **服务可用性：** ≥99.5%
- **错误率：** ≤0.1%
- **故障恢复时间：** ≤5 分钟
- **数据完整性：** 100%（无请求/响应丢失）

### 5.3 安全要求

- **API Key 保护：** 不在日志中输出完整 API key
- **传输安全：** 支持 HTTPS（生产环境）
- **访问控制：** 可选的 IP 白名单（未来功能）
- **敏感信息：** 不记录用户对话内容（除非调试模式）

### 5.4 可维护性要求

- **日志系统：** 结构化日志，支持日志级别
- **配置管理：** 环境变量配置，支持 .env 文件
- **错误处理：** 完整的异常捕获和错误信息
- **文档完整性：** README、调试指南、API 文档

### 5.5 兼容性要求

- **Python 版本：** ≥3.8
- **API 格式：** OpenAI 和 Anthropic 格式
- **操作系统：** Windows、Linux、macOS
- **客户端：** Claude Code、Cursor、任何支持自定义 API endpoint 的工具

---

## 6. 技术架构

### 6.1 系统架构

```
┌─────────────────┐
│  Claude Code    │
│  / 其他客户端   │
└────────┬────────┘
         │ HTTP Request
         ↓
┌─────────────────────────────────────┐
│      API 智能路由代理               │
│  ┌─────────────────────────────┐   │
│  │  请求接收层                 │   │
│  │  - 路由匹配                 │   │
│  │  - 请求解析                 │   │
│  └──────────┬──────────────────┘   │
│             ↓                       │
│  ┌─────────────────────────────┐   │
│  │  路由决策层                 │   │
│  │  - 检测 tools 字段          │   │
│  │  - 选择目标 API             │   │
│  └──────────┬──────────────────┘   │
│             ↓                       │
│  ┌─────────────────────────────┐   │
│  │  请求转发层                 │   │
│  │  - 设置认证头               │   │
│  │  - 转发请求                 │   │
│  │  - 处理响应                 │   │
│  └──────────┬──────────────────┘   │
│             ↓                       │
│  ┌─────────────────────────────┐   │
│  │  监控统计层                 │   │
│  │  - 请求计数                 │   │
│  │  - 日志记录                 │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
         │                    │
         ↓                    ↓
┌─────────────────┐  ┌─────────────────┐
│   官方 API      │  │   逆向 API      │
│  (支持 tools)   │  │  (纯对话)       │
└─────────────────┘  └─────────────────┘
```

### 6.2 技术栈

- **Web 框架：** FastAPI
- **HTTP 客户端：** httpx（异步）
- **配置管理：** python-dotenv
- **日志系统：** Python logging
- **部署：** Uvicorn

### 6.3 数据流

```
1. 客户端请求 → 代理服务
2. 解析请求体 JSON
3. 检查 tools 字段
4. 选择目标 API
5. 设置认证头
6. 转发请求
7. 接收响应
8. 返回给客户端
9. 记录统计信息
```

---

## 7. 实现计划

### 7.1 第一阶段：MVP（已完成）

**时间：** 1 天

**功能：**
- ✅ 基础路由逻辑
- ✅ 请求转发
- ✅ 响应返回
- ✅ 流式支持
- ✅ 健康检查
- ✅ 基础日志

### 7.2 第二阶段：监控增强（已完成）

**时间：** 1 天

**功能：**
- ✅ 调试模式
- ✅ 统计接口
- ✅ 配置检查
- ✅ 请求追踪
- ✅ 测试脚本
- ✅ 调试文档

### 7.3 第三阶段：生产就绪（待开发）

**时间：** 2-3 天

**功能：**
- ⏳ 性能优化
- ⏳ 错误重试机制
- ⏳ 请求缓存
- ⏳ 负载均衡
- ⏳ Docker 部署
- ⏳ 监控告警

### 7.4 第四阶段：高级功能（待规划）

**时间：** 待定

**功能：**
- ⏳ 多 API 配置（支持多个逆向 API）
- ⏳ 智能降级（官方 API 失败时降级到逆向）
- ⏳ 成本统计（按 token 计算成本）
- ⏳ Web 管理界面
- ⏳ API 密钥轮换
- ⏳ 访问控制（IP 白名单、API key 认证）

---

## 8. 风险和限制

### 8.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| 逆向 API 不稳定 | 高 | 中 | 实现降级机制，失败时切换到官方 API |
| 路由判断错误 | 高 | 低 | 完善测试用例，增加日志监控 |
| 性能瓶颈 | 中 | 低 | 异步处理，连接池优化 |
| 内存泄漏 | 中 | 低 | 定期重启，监控内存使用 |

### 8.2 业务风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| 逆向 API 被封禁 | 高 | 中 | 准备多个备用 API |
| 官方 API 价格上涨 | 中 | 低 | 持续优化路由策略 |
| 用户隐私问题 | 高 | 低 | 不记录对话内容，仅记录元数据 |

### 8.3 限制条件

- **逆向 API 限制：** 不支持 tool calling，功能受限
- **延迟增加：** 代理层会增加 20-50ms 延迟
- **单点故障：** 代理服务故障会影响所有请求
- **配置复杂度：** 需要配置多个 API endpoint 和 key

---

## 9. 测试计划

### 9.1 单元测试

- 路由决策逻辑
- 请求头设置
- 错误处理

### 9.2 集成测试

- 端到端请求流程
- 流式响应
- 错误场景

### 9.3 性能测试

- 并发压力测试
- 延迟测试
- 内存泄漏测试

### 9.4 兼容性测试

- Claude Code 集成
- Cursor 集成
- 不同 API 格式

---

## 10. 部署方案

### 10.1 本地部署（当前）

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env

# 3. 启动服务
python api_proxy.py
```

### 10.2 Docker 部署（未来）

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "api_proxy.py"]
```

### 10.3 云部署（未来）

- **选项 1：** AWS EC2 + Nginx
- **选项 2：** Google Cloud Run
- **选项 3：** Heroku
- **选项 4：** Railway

---

## 11. 文档清单

- ✅ **README.md** - 快速开始指南
- ✅ **DEBUG_GUIDE.md** - 调试指南
- ✅ **PRD.md** - 产品需求文档（本文档）
- ⏳ **API_REFERENCE.md** - API 接口文档
- ⏳ **DEPLOYMENT.md** - 部署指南
- ⏳ **CHANGELOG.md** - 版本更新日志

---

## 12. 附录

### 12.1 术语表

- **Tool Calling：** AI 模型调用外部工具的能力
- **逆向 API：** 非官方的第三方 API，通常成本更低但功能受限
- **流式响应：** Server-Sent Events，数据逐块返回
- **路由：** 根据规则将请求转发到不同的目标

### 12.2 参考资料

- [FastAPI 文档](https://fastapi.tiangolo.com/)
- [Anthropic API 文档](https://docs.anthropic.com/)
- [OpenAI API 文档](https://platform.openai.com/docs/)
- [httpx 文档](https://www.python-httpx.org/)

### 12.3 变更历史

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|----------|
| v1.0 | 2026-05-11 | - | 初始版本 |

---

**文档结束**
