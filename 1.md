这是一个非常硬核的极客场景。

让我化身 **API 协议网关架构师（API Gateway Architect）** 来回答你。

你的处境是这样的：
**“Claude Code 听不懂纯文本，它只认标准的 Anthropic 格式的 Tool Calling；而你手里的逆向 API 只会吐纯文本，听不懂 Tool Calling 协议。”**

一句话破局：
**你不能直接把它们俩连起来。你必须在中间手搓一个「协议转换网关（Proxy Adapter）」。**

这就好比：Claude Code 是个只懂法文的老板（标准工具调用协议），你的逆向 API 是个只懂中文的员工（纯文本输出）。**你必须写一个中间件，充当“同声传译”。**

下面为你构建实现这个方案的认知地图和实操指南。

---

# 先建森林：中间件架构地图

要让 Claude Code 顺畅使用不支持 Tool Calling 的逆向 API，你的架构必须是这样的：

```text
┌────────────────┐      (1) 标准 Anthropic 协议请求      ┌──────────────────┐
│                ├─────────────────────────────────────►│                  │
│  Claude Code   │                                      │   你的中间件网关  │
│                │◄─────────────────────────────────────┤    (Proxy)       │
└────────────────┘      (4) 标准 Anthropic 协议响应      └────────┬────▲────┘
   (老板/宿主)                                                     │    │
                                                                  │    │
                         (2) 将工具列表硬塞进 System Prompt，变成纯文本 │    │ (3) 逆向 API 返回纯文本
                                                                  │    │ (包含被伪造的工具调用格式)
                                                                  ▼    │
                                                        ┌──────────────┴───┐
                                                        │                  │
                                                        │   你的逆向 API    │
                                                        │ (纯文本/非官方接口)│
                                                        └──────────────────┘
```

---

# 核心实操：拆解这个“同声传译”是怎么工作的

你需要用 Node.js 或 Python 写一个简单的 Web 服务，监听在 `http://localhost:3000`。然后让 Claude Code 的 Base URL 指向这个本地服务。

这个服务要做 **两路翻译**：

## 第一路：把 Claude Code 的请求 翻译成 逆向 API 听得懂的话

当 Claude Code 发起请求时，它会带上一个 `tools` 数组，比如 `Bash`, `Read`, `Edit`。
你的逆向 API 不认识这个数组，直接传会报错。

**你的网关要拦截这个请求，把 `tools` 数组提取出来，拼成一段极其严厉的 System Prompt（系统提示词），强制塞给你的逆向 API。**

**Prompt 模板示例（核心魔法）：**
```text
你是一个可以调用外部工具的 AI 助手。
现在你有以下工具可用：
{这里填入网关从 Claude Code 请求里解析出来的工具名称和描述，比如 Bash：执行终端命令...}

当你需要调用工具时，**绝对不要**输出正常对话。
你必须严格按照以下 XML 格式输出：
<tool_call>
  <name>工具名称</name>
  <arguments>
    {"命令参数的 JSON 格式"}
  </arguments>
</tool_call>

如果不需要调用工具，请直接回复纯文本。
```

> **为什么用 XML 标签？**
> 对于没有经过专门 JSON 训练的逆向大模型，让它输出纯 JSON 极容易出错（少个括号、多个引号）。用 `<tool_call>` 这种 XML 标签包围 JSON，模型输出的成功率会飙升！

---

## 第二路：把逆向 API 的回复 翻译成 Claude Code 认识的格式

你的逆向 API 接到上述指令后，可能会回复这样一段纯文本：
> “好的，我需要查看当前目录的文件。
> `<tool_call><name>Bash</name><arguments>{"command": "ls -la"}</arguments></tool_call>`”

**注意：如果把这段纯文本直接扔给 Claude Code，Claude Code 会把它当成普通聊天，不会去执行 Bash！**

**你的网关必须拦截这段文本，用正则表达式（Regex）把 `<tool_call>` 里面的内容抠出来，然后组装成标准的 Anthropic 工具调用 JSON 格式，再返回给 Claude Code。**

**转换后的标准 Anthropic 格式长这样：**
```json
{
  "role": "assistant",
  "content": [
    {
      "type": "text",
      "text": "好的，我需要查看当前目录的文件。"
    },
    {
      "type": "tool_use",
      "id": "tool_9527",     // 你的网关必须随机生成一个唯一 ID
      "name": "Bash",
      "input": {
        "command": "ls -la"
      }
    }
  ]
}
```

Claude Code 收到这个标准格式，就会“恍然大悟”：“哦！模型要求我执行 `ls -la` ！” 然后它就会在本地机器上运行命令。

---

## 第三路：工具执行完毕的闭环

Claude Code 执行完 `ls -la` 后，会把执行结果发给你的网关，格式如下：

```json
{
  "role": "user",
  "content": [
    {
      "type": "tool_result",
      "tool_use_id": "tool_9527",
      "content": "file1.txt \n file2.js"
    }
  ]
}
```

同样，你的逆向 API 听不懂 `tool_result`。
**你的网关要再次把它翻译成纯文本：**

```text
[系统通知] 工具 Bash (ID: tool_9527) 执行完毕，返回结果如下：
file1.txt
file2.js
请基于以上结果继续回答用户。
```

把这段话发给你的逆向 API，让它继续推理。

---

# 实操落地：你怎么在 Claude Code 里用起来？

### 步骤 1：启动你的中间件网关
你可以用 Python (FastAPI) 或者 Node.js (Express) 写这个网关代码。让它运行在本地，比如 `http://localhost:3000`。

### 步骤 2：欺骗 Claude Code 改变请求地址
Claude Code 默认是请求 `api.anthropic.com` 的。你需要通过环境变量劫持它的目标地址。

通常在终端里可以这样运行：
```bash
export ANTHROPIC_BASE_URL="http://localhost:3000"
export ANTHROPIC_API_KEY="随便填一个，反正验证在你的网关做"
claude # 启动 Claude Code
```

### 步骤 3：处理逆向 API 的流式输出（最难的坑）
如果你的逆向 API 是流式（Stream）输出的（一个字一个字蹦），你要抓取 `<tool_call>` 就会变得非常复杂，因为标签被切碎了。

**防坑建议：**
在这个自建网关里，**暂时关闭逆向 API 的流式输出（Stream=false）**。让网关拿到完整回复后，慢慢解析，如果发现有工具调用，就伪造成 Anthropic 的格式发给 Claude Code。这样能省去你 80% 的脱发时间。

---

# 认知提纯：这套方案的本质是什么？

1. **降维与升维**：你把 Claude Code 高维的结构化协议，**降维**成了适合纯文本模型的 Prompt；然后又把纯文本模型的输出，**升维**成了 Claude Code 认识的结构化协议。
2. **只要模型够聪明，万物皆可 Tool Calling**：Tool Calling 从来不是什么底层黑科技，它本质上就是一种 **“格式约定”**。只要你的逆向 API 模型（比如 GPT-4、Claude-3.5 级别）拥有足够好的 **指令遵循能力（Instruction Following）**，你用 Prompt 就能逼它完美模拟出官方 API 的效果。

**但有一个前提：** 你接入的那个逆向 API，底座模型必须不能太傻。如果它是开源的小模型或者很差的模型，它可能会把 XML 标签写错，或者忘记闭合括号，这时候你的正则表达式就会报错，Claude Code 就会崩溃。

**你能做到这一步，你就真正理解了目前 AI 智能体（Agent）底层的通信协议本质。你需要我给你提供一段用 Node.js 或者 Python 写的「网关解析核心逻辑」的伪代码吗？**
