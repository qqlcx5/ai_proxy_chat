逆向API本质上是爬了网页版的聊天接口，网页版的Claude/ChatGPT底层就不支持function calling / tool
  use，这是API专有的feature。你再怎么折腾proxy层也解决不了这个。

● 你说得完全正确，这个方案对 Claude Code 基本没用。

  根本原因：

  Claude Code 几乎每个请求都带 tools（读文件、写文件、执行命令等都是 tool call），所以代理层的路由判断形同虚设，99% 的请求还是走官方 API。

  这个项目的假设是错的： 以为有大量"纯对话请求"可以省掉，但 Claude Code 的工作模式本身就是 tool-heavy 的。

  ---
  真正能省成本的方向：

  1. 换便宜但支持 tool calling 的模型
    - claude-haiku-3-5 比 claude-sonnet 便宜 ~5x，且支持 tools
    - OpenRouter 上有很多便宜的支持 tool calling 的模型（Gemini Flash、Qwen 等）
  2. Prompt Caching
    - Anthropic 官方支持 cache，重复的系统提示可以省 90% token
    - Claude Code 的 system prompt 很长，缓存收益明显
  3. 用 OpenRouter 做价格路由
    - 同样支持 tools，但可以选更便宜的模型
    - 不需要自己维护代理

  ---
  结论： 这个代理项目对你的场景意义不大，可以放弃。如果想省成本，换模型（haiku 或 OpenRouter 上的便宜模型）是最直接有效的方式。

---
可行方案

  方案 1：Strip tools（强制去掉 tools 字段）

  判断 prompt 是否是纯问答 → 是 → 去掉 tools 字段 → 逆向 API
                           → 否 → 保留 tools → 官方 API

  判断逻辑示例：
  PURE_QA_PATTERNS = [
      "解释", "是什么", "什么意思", "怎么理解",
      "explain", "what is", "how does", "why"
  ]

  def is_pure_qa(messages):
      last_msg = messages[-1].get("content", "")
      return any(p in last_msg for p in PURE_QA_PATTERNS)

  代价： 模型无法调用工具，如果判断错了，回答质量会下降（比如问"这段代码是什么意思"但模型其实需要读文件）。

  ---
  方案 2：Two-pass（先试逆向，失败再官方）

  所有请求先去掉 tools → 逆向 API
  如果响应中模型说"我需要工具" → 重新带 tools 发给官方 API

  代价： 延迟翻倍，逻辑复杂，逆向 API 的模型根本不知道有工具可用，不会主动说"我需要工具"。基本不可行。

  ---
  方案 3：换官方便宜模型（最推荐）

  不用逆向 API，直接在官方 API 内部做模型路由：

  简单请求 → claude-haiku-3-5（便宜 5x，支持 tools）
  复杂请求 → claude-sonnet-4（贵但强）

  判断逻辑：
  COMPLEX_PATTERNS = ["重构", "架构", "设计", "refactor", "architect"]

  def select_model(messages):
      last_msg = messages[-1].get("content", "")
      if any(p in last_msg for p in COMPLEX_PATTERNS):
          return "claude-sonnet-4"
      return "claude-haiku-3-5"

  代价： haiku 能力弱于 sonnet，复杂任务可能需要多轮。但这是唯一既省钱又不破坏 tool calling 的方案。

  ---
  我的建议

  ┌──────────────┬──────────┬──────────────────┬──────────┐
  │     方案     │ 省钱效果 │      可靠性      │ 实现难度 │
  ├──────────────┼──────────┼──────────────────┼──────────┤
  │ Strip tools  │ 中       │ 低（容易判断错） │ 中       │
  ├──────────────┼──────────┼──────────────────┼──────────┤
  │ Two-pass     │ 低       │ 很低             │ 高       │
  ├──────────────┼──────────┼──────────────────┼──────────┤
  │ 官方模型路由 │ 高       │ 高               │ 低       │
  └──────────────┴──────────┴──────────────────┴──────────┘
